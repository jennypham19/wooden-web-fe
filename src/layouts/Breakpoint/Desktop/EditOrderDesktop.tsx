import { Paper } from "@mui/material";
import Grid from "@mui/material/Grid2";
interface EditOrderDesktopProps{

}
const EditOrderDesktop = (props: EditOrderDesktopProps) => {
    const { } = props;
    return(
        <Grid container spacing={2}>
            <Grid size={{ xs: 12 }}>
                <Paper sx={{ p: 2 }}>
                    
                </Paper>
            </Grid>
        </Grid>
    )
}

export default EditOrderDesktop;