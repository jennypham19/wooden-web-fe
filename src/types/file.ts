export interface IInputFile{
    id: string,
    name: string,
    url: string,
    createdAt?: string,
    updatedAt?: string
}

export interface IReferenceLink {
    id: string,
    name?: string,
    url: string,
    createdAt?: string,
    updatedAt?: string
}